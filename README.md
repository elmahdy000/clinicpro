# ClinicPro Monorepo

This workspace contains a NestJS `api/` backend and a Next.js `web/` frontend (created earlier).

Quick start (after installing dependencies):

```powershell
cd d:\clinicpro_app
npm install
npm run dev
```

API health: `GET /api/health`
